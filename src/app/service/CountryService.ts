import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import CountryAPIResponse from './CountryData';

export default class CountryService {
  //properties
  //constructor
  // constructor(private http: HttpClient) {}
  //custom methods
  // public getCountryData(country: string): any {
  //   // const url: string = `restcountries.com/v3.1/name/${country}`;
  //   // return this.http.get<string>(url);
  //   const mockResponse: CountryAPIResponse = {
  //     title: 'title',
  //     subtitle: 'subtitle',
  //   };
  //   return mockResponse;
  // }
}
